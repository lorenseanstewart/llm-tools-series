{
  "name": "mcp-analytics-server",
  "version": "0.0.1",
  "description": "MCP server for real estate analytics",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "start:dev": "tsx watch src/server.ts",
    "dev": "tsx watch src/server.ts",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage"
  },
  "dependencies": {
    "@fastify/cors": "^8.4.0",
    "@llm-tools/shared-types": "^0.0.1",
    "axios": "^1.10.0",
    "fastify": "^4.24.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "tsx": "^4.20.3",
    "typescript": "^5.7.3"
  }
}
